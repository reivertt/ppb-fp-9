import 'package:flutter/material.dart';

class PlantsItem extends StatelessWidget {
  const PlantsItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Plants Page - plants_item.dart'));
  }
}
